Task:
1. Write a Python Program to perform dimensionality on any common dataset such as Iris
dataset and a dataset of any domain with high dimension.
2. Choose a supervised machine learning model, train and test the model by giving
a. Dataset without applying PCA on both the datasets
b. Apply PCA , LDA , SVD , MDS and tSNE on both the dataset and then train the model
3. Note the accuracy of the model – with PCA and without PCA
4. Analyse the result. Write in brief a paragraph on your observations.
